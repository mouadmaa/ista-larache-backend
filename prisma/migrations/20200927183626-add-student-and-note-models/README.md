# Migration `20200927183626-add-student-and-note-models`

This migration has been generated by mouadmaa at 9/27/2020, 7:36:26 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."Student" (
"id" text   NOT NULL ,
"name" text   NOT NULL ,
"cef" text   ,
"cin" text   ,
"password" text   NOT NULL ,
"dateBirth" timestamp(3)   NOT NULL ,
"finalNote1" Decimal(65,30)   ,
"finalNote2" Decimal(65,30)   ,
"classId" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Note" (
"id" text   NOT NULL ,
"note1" Decimal(65,30)   ,
"note2" Decimal(65,30)   ,
"note3" Decimal(65,30)   ,
"efm" Decimal(65,30)   ,
"studentId" text   NOT NULL ,
"moduleId" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "Student.cef_unique" ON "public"."Student"("cef")

CREATE UNIQUE INDEX "Student.cin_unique" ON "public"."Student"("cin")

CREATE UNIQUE INDEX "Note.studentId_moduleId_unique" ON "public"."Note"("studentId", "moduleId")

ALTER TABLE "public"."Student" ADD FOREIGN KEY ("classId")REFERENCES "public"."Class"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Note" ADD FOREIGN KEY ("studentId")REFERENCES "public"."Student"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Note" ADD FOREIGN KEY ("moduleId")REFERENCES "public"."Module"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200927133251-add-unique-name-to-formation..20200927183626-add-student-and-note-models
--- datamodel.dml
+++ datamodel.dml
@@ -2,13 +2,14 @@
 // learn more about it in the docs: https://pris.ly/d/prisma-schema
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
+  previewFeatures = ["transactionApi"]
 }
 model User {
   id        String   @id @default(cuid())
@@ -18,8 +19,22 @@
   password  String
   role      Role     @default(TEACHER)
 }
+model Student {
+  id          String   @id @default(cuid())
+  name        String
+  cef         String?  @unique
+  cin         String?  @unique
+  password    String
+  dateBirth   DateTime
+  notes       Note[]
+  finalNote1  Float?
+  finalNote2  Float?
+  class       Class    @relation(fields: [classId], references: [id])
+  classId     String
+}
+
 model Formation {
   id        String   @id @default(cuid())
   name      String   @unique
   descUrl   String
@@ -50,8 +65,22 @@
   @@unique([number, formationId])
   @@unique([name, formationId])
 }
+model Note {
+  id        String   @id @default(cuid())
+  note1     Float?
+  note2     Float?
+  note3     Float?
+  efm       Float?
+  student   Student  @relation(fields: [studentId], references: [id])
+  studentId String
+  module    Module   @relation(fields: [moduleId], references: [id])
+  moduleId  String
+
+  @@unique([studentId, moduleId])
+}
+
 enum Role {
   ADMIN
   TEACHER
 }
```


